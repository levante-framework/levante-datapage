on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

name: Update overview data

permissions:
  actions: write

jobs:
  update-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install r-base-dev libcurl4-openssl-dev libudunits2-dev libgdal-dev

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.5.1'

      - name: Set up renv
        uses: r-lib/actions/setup-renv@v2

      - name: Run processed_data_overview Redivis workflow
        run: Rscript scripts/update_overview_data.R
        env:
          REDIVIS_API_TOKEN: ${{ secrets.REDIVIS_API_TOKEN }}

      - name: Trigger site rebuild
        run: gh workflow run quarto-publish.yaml --ref main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
